# .env.example - Local development configuration template
ENVIRONMENT=local
DEBUG=true
SECRET_KEY=dev-secret-change-me

# Database configuration (use same values for local Docker setup)
DB_ENGINE=postgres
DB_NAME=analytics
DB_USER=analytics
DB_PASSWORD=analytics
DB_HOST=localhost
DB_PORT=5432

# Storage configuration
USE_GCS=false

# Other settings
ALLOWED_HOSTS=*
LOG_LEVEL=DEBUG